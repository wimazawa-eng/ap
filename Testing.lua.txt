if getgenv().aural then return end
getgenv().aural = true

local Players = game:GetService("Players")
local LocalPlayer = Players.LocalPlayer
local Character = LocalPlayer.Character
local Camera = workspace.CurrentCamera
local Humanoid = Character and Character:WaitForChild("Humanoid")
local HRP = Character and Character:WaitForChild("HumanoidRootPart")
local Input = game:GetService("VirtualInputManager")
local RunService = game:GetService("RunService")
local DeviceEvent = game.ReplicatedStorage:WaitForChild("Remotes"):WaitForChild("deviceEvent")
local rangeConnection
local autoQueueRunning = false
local oobCache = {}
local autoQueueThread = nil
local landedConnection
local UserInputService = game:GetService("UserInputService")
local SharedTableRegistry = game:GetService("SharedTableRegistry")
local ReplicatedStorage = game:GetService("ReplicatedStorage")
local PowerUI = LocalPlayer.PlayerGui:WaitForChild("PowerUI")
local Icon = PowerUI:WaitForChild("CursorIcon")
local CursorIcon2 = PowerUI:WaitForChild("CursorIcon2")

local indicator = Instance.new("Highlight")
indicator.Name = "CoreGui"
indicator.Parent = nil
indicator.FillColor = Color3.fromRGB(255, 0, 0)

local TrajColors = {
    Red    = Color3.fromRGB(255, 0, 0),
    Orange = Color3.fromRGB(255, 130, 0),
    Yellow = Color3.fromRGB(255, 255, 0),
    Green  = Color3.fromRGB(0, 255, 0),
    Blue   = Color3.fromRGB(0, 130, 255),
    Purple = Color3.fromRGB(170, 0, 255),
    White  = Color3.fromRGB(255, 255, 255),
    Rainbow = nil,
}

local Client = LocalPlayer
local Effects, EffectNames = {}, {}
local Balls, BallNames = {}, {}
local AuraList, AuraNames = {}, {"None"}
local goalEventHook = nil
local OriginalReplicateBall = nil
local Replicator = nil

local Trails = {}
local TrailNames = {}
pcall(function()
    local TrailFolder = ReplicatedStorage.ShopItems:FindFirstChild("Trail")
    if TrailFolder then
        for _, trail in TrailFolder:GetChildren() do
            local rawName = trail.Name
            local cleanName = rawName:gsub("^Trail_", "")
            Trails[cleanName] = trail
            table.insert(TrailNames, cleanName)
        end
        table.sort(TrailNames)
    end
end)
if #TrailNames == 0 then
    TrailNames = {"None"}
    Trails["None"] = nil
end

local GoalCheck = {}
for _,v in ipairs(Workspace:GetChildren()) do
    if v.Name == "PracticeArea" or v.Name == "Courts" then
        for _,b in ipairs(v:GetDescendants()) do
            if b.Name == "Swish" then
                table.insert(GoalCheck, b.Parent)
            end
        end
    end
end

local function FindNearestGoal()
    local NearestRim = nil
    local ShortestDistance = math.huge
    local IsEnemyGoal = false
    local HRPXZ = Vector3.new(HRP.Position.X, 0, HRP.Position.Z)
    for _, Goal in ipairs(GoalCheck) do
        local Hitboxes = Goal.Parent
        if Hitboxes and (Hitboxes.Name == "HitboxesHome" or Hitboxes.Name == "HitboxesAway") then
            local Court = Hitboxes.Parent
            local CourtPlayers = Court:FindFirstChild("_CourtPlayers")
            if CourtPlayers then
                local MyTeam
                for _,v in ipairs(CourtPlayers:GetChildren()) do
                    local SlotTeam = v.Name:sub(1,4)
                    local SlotValue = v.Value
                    if typeof(SlotValue) == "Instance" and SlotValue:IsA("Player") then
                        if SlotValue.Name == LocalPlayer.Name then
                            MyTeam = (SlotTeam == "Home") and "Home" or (SlotTeam == "Away" and "Away")
                            break
                        end
                    end
                end
                if MyTeam then
                    if (MyTeam == "Home" and Hitboxes.Name == "HitboxesHome") or (MyTeam == "Away" and Hitboxes.Name == "HitboxesAway") then
                        continue
                    else
                        IsEnemyGoal = true
                    end
                end
            end
        end
        local GoalXZ = Vector3.new(Goal.Position.X, 0, Goal.Position.Z)
        local Dist = (HRPXZ - GoalXZ).Magnitude
        if Dist < ShortestDistance then
            ShortestDistance = Dist
            NearestRim = Goal
        end
    end
    return NearestRim, ShortestDistance, IsEnemyGoal
end

local Rim, Dist, IsEnemy = FindNearestGoal()

pcall(function()
    local EffectsFolder = ReplicatedStorage.ShopItems.Effect
    for _, eff in EffectsFolder:GetChildren() do
        local Name = eff.Name:gsub("Effect_", "")
        Effects[Name] = eff
        table.insert(EffectNames, Name)
    end
end)

pcall(function()
    Replicator = require(ReplicatedStorage.Util.Replicator)
    OriginalReplicateBall = Replicator["ReplicateBall"]
    for _, ballItem in ReplicatedStorage.ShopItems.Ball:GetChildren() do
        local Name = ballItem.Name:gsub("Ball_", "")
        Balls[Name] = ballItem.Name
        table.insert(BallNames, Name)
    end
end)

pcall(function()
    local ShopItems = ReplicatedStorage:FindFirstChild("ShopItems")
    local AuraFolder = ShopItems and ShopItems:FindFirstChild("Aura")
    if AuraFolder then
        for _, child in pairs(AuraFolder:GetChildren()) do
            if child:IsA("Model") then
                table.insert(AuraList, child)
                table.insert(AuraNames, child.Name)
            end
        end
    end
end)

local CourtNames = {"Court A","Court B","Court C","Court D"}
local Courts = workspace:FindFirstChild("Courts")
local currentCourtHitbox = nil
local autoQueueTask = nil

local function FindCourt()
    if not Courts then return nil end
    local bestCourt = nil
    local bestScore = 0
    for _, cn in pairs(CourtNames) do
        local Court = Courts:FindFirstChild(cn)
        if not Court then continue end
        local PlayersFolder = Court:FindFirstChild("_CourtPlayers")
        local SettingsFolder = Court:FindFirstChild("_CourtSettings")
        if not (PlayersFolder and SettingsFolder) then continue end
        local Home = PlayersFolder:FindFirstChild("Home1")
        local Away = PlayersFolder:FindFirstChild("Away1")
        local Queue = PlayersFolder:FindFirstChild("Queue1")
        local HomeScore = SettingsFolder:FindFirstChild("HomeScore")
        local AwayScore = SettingsFolder:FindFirstChild("AwayScore")
        if not (Home and Away and Queue) then continue end
        if Home.Value == nil or Away.Value == nil then continue end
        if Queue.Value ~= nil then continue end
        local Hitbox = Court:FindFirstChild("Hitbox", true)
        if not (Hitbox and Hitbox:IsA("BasePart")) then continue end
        if getgenv().Settings.Automatics.QueueAlmostFinished then
            local hScore = HomeScore and HomeScore.Value or 0
            local aScore = AwayScore and AwayScore.Value or 0
            local highest = math.max(hScore, aScore)
            if highest >= 15 and highest > bestScore then
                bestScore = highest
                bestCourt = Hitbox
            end
        else
            return Hitbox
        end
    end
    return bestCourt
end

local function AutoQueueLoop()
    while getgenv().Settings.Automatics.AutoQueue do
        task.wait(1)
        if not getgenv().Settings.Automatics.AutoQueueAfterGames and currentCourtHitbox then continue end
        local char = LocalPlayer.Character
        if not char then continue end
        local hrp = char:FindFirstChild("HumanoidRootPart")
        if not hrp then continue end
        local targetHitbox = FindCourt()
        if not targetHitbox then continue end
        if targetHitbox == currentCourtHitbox then
            if not getgenv().Settings.Automatics.AutoQueueAfterGames then currentCourtHitbox = targetHitbox end
            continue
        end
        local pos = targetHitbox.Position + Vector3.new(0, 3, 0)
        if getgenv().Settings.Automatics.JoinType == "Teleport" then
            hrp.CFrame = CFrame.new(pos)
        else
            local hum = char:FindFirstChild("Humanoid")
            if hum then hum:MoveTo(pos) end
        end
        currentCourtHitbox = targetHitbox
    end
end

local function startAutoQueue()
    if autoQueueTask then return end
    currentCourtHitbox = nil
    autoQueueTask = task.spawn(AutoQueueLoop)
end

local function stopAutoQueue()
    if autoQueueTask then task.cancel(autoQueueTask) autoQueueTask = nil end
    currentCourtHitbox = nil
end

local shot_data = {[59]=82,[60]=80,[61]=81,[62]=78,[63]=77.8,[64]=74,[65]=73.8,[66]=70,[67]=69.8,[68]=65,[69]=61,[70]=58,[71]=50,[72]=50,[73]=50}
local inner_arc_data = {
    [7]=122,[8]=121,[9]=120,[10]=119,[11]=118,[12]=117,[13]=116,[14]=115,[15]=114,[16]=113,[17]=112,[18]=111,[19]=110,[20]=109,[21]=108,[22]=107,
    [23]=106,[24]=105,[25]=104,[26]=104,[27]=105,[28]=105,[29]=106,[30]=106,[31]=105,[32]=104,[33]=104,[34]=103,[35]=102,[36]=102,[37]=101,[38]=101,
    [39]=100,[40]=100,[41]=99,[42]=98,[43]=98,[44]=97,[45]=96,[46]=95,[47]=94,[48]=94,[49]=93,[50]=92,[51]=90,[52]=89,[53]=87,[54]=86,[55]=84,[56]=83,[57]=81,[58]=80
}

local mobileArcs = {
    [30]=37,[31]=37,[32]=37,[33]=37,[34]=37,[35]=37,[36]=37,[37]=37,[38]=37,[39]=37,
    [40]=38,[41]=37,[42]=37,[43]=37,[44]=37,[45]=37,[46]=37,[47]=37,[48]=37,[49]=37,
    [50]=37,[51]=36,[52]=36,[53]=36,[54]=35,[55]=33,[56]=33,[57]=33,[58]=33,[59]=30,
    [60]=30,[61]=30,[62]=30,[63]=28,[64]=28,[65]=28,[66]=25,[67]=25,[68]=23,[69]=23,
    [70]=20,[71]=20,[72]=18,[73]=18
}

local function getArcOffset(dist)
    local isMobile = UserInputService.TouchEnabled
    if isMobile then
        local d = math.floor(dist + 0.5)
        if d < 30 then d = 30 end
        if d > 73 then d = 73 end
        return mobileArcs[d] or 37
    end
    if inner_arc_data[dist] then return inner_arc_data[dist] end
    if shot_data[dist] then return shot_data[dist] end
    if dist < 7 then return 122 end
    if dist > 73 then return math.max(20, 50 - (dist - 73) * 1.5) end
    local lower, upper = nil, nil
    for d, v in pairs(inner_arc_data) do
        if d <= dist and (not lower or d > lower) then lower = d end
        if d >= dist and (not upper or d < upper) then upper = d end
    end
    for d, v in pairs(shot_data) do
        if d <= dist and (not lower or d > lower) then lower = d end
        if d >= dist and (not upper or d < upper) then upper = d end
    end
    if lower and upper and lower ~= upper then
        local lowerVal = inner_arc_data[lower] or shot_data[lower] or 0
        local upperVal = inner_arc_data[upper] or shot_data[upper] or 0
        local t = (dist - lower) / (upper - lower)
        return lowerVal + (upperVal - lowerVal) * t
    elseif lower then
        return inner_arc_data[lower] or shot_data[lower] or 0
    elseif upper then
        return inner_arc_data[upper] or shot_data[upper] or 0
    else
        return 100
    end
end

local function getArc(dist, power)
    local isMobile = UserInputService.TouchEnabled
    if isMobile then
        local d = math.floor(dist + 0.5)
        if d < 30 then d = 30 end
        if d > 73 then d = 73 end
        local base = mobileArcs[d] or 37
        if power ~= 85 then base = base * (85 / power) end
        return base
    end
    if inner_arc_data[dist] then
        local base = inner_arc_data[dist]
        if power ~= 85 then base = base * (85 / power) end
        return base
    end
    local baseArc
    if shot_data[dist] then
        baseArc = shot_data[dist]
    elseif dist < 73 then
        local nearestDist = nil
        local nearestDiff = math.huge
        for d, v in pairs(shot_data) do
            local diff = math.abs(d - dist)
            if diff < nearestDiff then nearestDiff = diff nearestDist = d baseArc = v end
        end
    else
        local extrapolated = 50 - (dist - 73) * 1.5
        baseArc = math.max(20, extrapolated)
    end
    if power ~= 85 then baseArc = baseArc * (85 / power) end
    return baseArc
end

getgenv().Settings = {
    Shooting = {
        Mode = "Aural",
        AuralEnabled = false,
        FatalEnabled = false,
        ShotDelay = 0.32,
        ShotYAxis = 0,
        RandomizeShotDelay = false,
        ShotDelayMin = 0.25,
        ShotDelayMax = 0.38,
        GreenChance = 0,
        ThreesOnly = false,
        AimAssist = false,
    },
    Defending = {
        AutoGuard = false,
        AutoGuardNearest = false,
        AutoGuardKeybind = "K",
        AutoGuardRange = 33,
        GuardPrediction = 0.58,
        SpacingDistance = 3,
        ReactionTime = 0,
        JumpIfThrown = false
    },
    Automatics = {
        AutoQueue = false,
        QueueAlmostFinished = false,
        JoinType = "Teleport",
        AutoQueueAfterGames = false
    },
    Visuals = {
        VisualizeBallProjectory = false,
        ProjectoryColor = "Red",
        ShotIndicator = false,
        ShowHiddenRanks = false,
        IncludeYourself = false,
        HidePlayerTags = false,
    },
    Ball = {
        BallMags = false,
        BallMagRange = 15,
        BallMagVisualizer = false,
        BallReach = false,
        BallReachRange = 0,
        MagnetKeybind = "M",
    },
    Player = {
        Walkspeed = false,
        WSvalue = 2
    },
    Misc = {
        AntiTravel = false,
        AntiOOB = false,
        AntiModerator = false,
        Type = "Notify",
        DeviceSpoofer = false,
        Device = "PC"
    },
    Goal = {
        Enabled = false,
        SelectedEffect = "LightRay",
        CurrentEffect = "LightRay"
    },
    BallChanger = {
        Enabled = false,
        SelectedBall = "ForceField",
        CurrentBall = "ForceField"
    },
    Aura = {
        Enabled = false,
        SelectedAura = "None",
        CurrentAura = nil
    },
    TrailChanger = {
        Enabled = false,
        SelectedTrail = (#TrailNames > 0 and TrailNames[1] ~= "None") and TrailNames[1] or "None",
        CurrentTrail = (#TrailNames > 0 and TrailNames[1] ~= "None") and TrailNames[1] or "None"
    }
}

local s = getgenv().Settings

local function getClosestDribbler()
    local char = LocalPlayer.Character
    local root = char and char:FindFirstChild("HumanoidRootPart")
    if not root then return nil end
    local closestPlayer, closestDist = nil, math.huge
    for _, part in workspace:GetPartBoundsInRadius(root.Position, s.Defending.AutoGuardRange) do
        if part:IsA("BasePart") and part.Name == "Ball" and part.Parent:IsA("Folder") then
            local holder = part.Parent.Parent
            local holderRoot = holder and holder:FindFirstChild("HumanoidRootPart")
            if holderRoot then
                local dist = (holderRoot.Position - root.Position).Magnitude
                if dist < closestDist then
                    closestDist = dist
                    for _, plr in Players:GetPlayers() do
                        if plr.Character == holder then closestPlayer = plr break end
                    end
                end
            end
        end
    end
    return closestPlayer
end

local function getClosestPlayer()
    local char = LocalPlayer.Character
    local root = char and char:FindFirstChild("HumanoidRootPart")
    if not root then return nil end
    local closestPlayer, closestDist = nil, math.huge
    for _, plr in Players:GetPlayers() do
        if plr == LocalPlayer then continue end
        local c = plr.Character
        local hrp = c and c:FindFirstChild("HumanoidRootPart")
        if not hrp then continue end
        local dist = (hrp.Position - root.Position).Magnitude
        if dist < s.Defending.AutoGuardRange and dist < closestDist then
            closestDist = dist closestPlayer = plr
        end
    end
    return closestPlayer
end

local function getGuardPosition(target)
    if LocalPlayer.Character and LocalPlayer.Character:FindFirstChild("Basketball") then return nil end
    local targetChar = target.Character
    local targetBall = targetChar and targetChar:FindFirstChild("Basketball") and targetChar.Basketball:FindFirstChild("Ball")
    if not targetBall then return nil end
    local targetHrp = targetChar:FindFirstChild("HumanoidRootPart")
    local targetHum = targetChar:FindFirstChild("Humanoid")
    local myHrp = LocalPlayer.Character and LocalPlayer.Character:FindFirstChild("HumanoidRootPart")
    local myHum = LocalPlayer.Character and LocalPlayer.Character:FindFirstChild("Humanoid")
    if not targetHrp or not targetHum or not myHrp or not myHum then return nil end
    local ballPos = targetBall.Position
    local vel = targetHrp.AssemblyLinearVelocity
    local moveDir = targetHum.MoveDirection
    local distance = (ballPos - myHrp.Position).Magnitude
    local timeToReach = distance / (myHum.WalkSpeed + 10)
    local predictedMove = moveDir * targetHum.WalkSpeed * s.Defending.GuardPrediction + Vector3.new(vel.X, 0, vel.Z) * 0.5
    local interceptPos = targetHrp.Position + predictedMove * timeToReach
    local facing = moveDir.Magnitude > 0 and moveDir.Unit or Vector3.new(0, 0, 1)
    return Vector3.new(interceptPos.X + facing.X * 2, ballPos.Y, interceptPos.Z + facing.Z * 2)
end

local magnetHitbox = nil
local magnetConnection = nil

local function createMagnetHitbox(character)
    if magnetHitbox then magnetHitbox:Destroy() magnetHitbox = nil end
    if character:FindFirstChild("FatalMagnet") then character.FatalMagnet:Destroy() end
    local head = character:FindFirstChild("Head")
    if not head then return end
    magnetHitbox = Instance.new("Part")
    magnetHitbox.Name = "FatalMagnet"
    magnetHitbox.Shape = Enum.PartType.Ball
    magnetHitbox.Size = Vector3.new(s.Ball.BallMagRange, s.Ball.BallMagRange, s.Ball.BallMagRange)
    magnetHitbox.Transparency = s.Ball.BallMagVisualizer and 0.65 or 1
    magnetHitbox.CanCollide = false
    magnetHitbox.Massless = true
    magnetHitbox.Parent = character
    local weld = Instance.new("Weld")
    weld.Part0 = head
    weld.Part1 = magnetHitbox
    weld.C0 = CFrame.new(0, 0, 0)
    weld.Parent = magnetHitbox
    magnetHitbox.Touched:Connect(function(part)
        if not s.Ball.BallMags or LocalPlayer.Character and LocalPlayer.Character:FindFirstChild("Basketball") then return end
        if part.Name == "Ball" or (part.Parent and part.Parent.Name == "Basketball") then
            firetouchinterest(head, part, 0)
            task.wait()
            firetouchinterest(head, part, 1)
        end
    end)
end

local function enableMagnet()
    local char = LocalPlayer.Character
    if not char then return end
    createMagnetHitbox(char)
    if magnetConnection then magnetConnection:Disconnect() magnetConnection = nil end
    magnetConnection = RunService.Heartbeat:Connect(function()
        if not s.Ball.BallMags then return end
        local character = LocalPlayer.Character
        if character and not character:FindFirstChild("FatalMagnet") then createMagnetHitbox(character) end
    end)
end

local function disableMagnet()
    if magnetConnection then magnetConnection:Disconnect() magnetConnection = nil end
    if magnetHitbox then magnetHitbox:Destroy() magnetHitbox = nil end
    if LocalPlayer.Character and LocalPlayer.Character:FindFirstChild("FatalMagnet") then LocalPlayer.Character.FatalMagnet:Destroy() end
end

local function getTemplateTrail()
    local trailName = s.TrailChanger.CurrentTrail
    if not trailName or trailName == "None" then return nil end
    local trailModel = Trails[trailName]
    if not trailModel then return nil end
    if trailModel:IsA("BasePart") then
        local t = trailModel:FindFirstChildOfClass("Trail")
        if t then return t end
    end
    local t = trailModel:FindFirstChildOfClass("Trail")
    if t then return t end
    for _, desc in trailModel:GetDescendants() do
        if desc:IsA("Trail") then return desc end
    end
    return nil
end

local function cleanBallTrails(ballPart)
    if not ballPart then return end
    for _, child in ballPart:GetChildren() do
        if child.Name == "AuralAttach0" or child.Name == "AuralAttach1" then
            child:Destroy()
        end
        if child:IsA("Trail") and child.Name == "AuralTrail" then
            child:Destroy()
        end
    end
end

local function restoreTrailColor(ballPart)
    if not s.TrailChanger.Enabled then return end
    local templateTrail = getTemplateTrail()
    if not templateTrail then return end
    if ballPart then
        for _, child in ballPart:GetChildren() do
            if child:IsA("Trail") and child.Name == "AuralTrail" then
                child.Color = templateTrail.Color
                child.LightEmission = templateTrail.LightEmission
                child.Transparency = templateTrail.Transparency
                child.Enabled = true
            end
        end
    else
        for _, desc in workspace:GetDescendants() do
            if desc:IsA("BasePart") and desc.Name == "Ball" then
                for _, child in desc:GetChildren() do
                    if child:IsA("Trail") and child.Name == "AuralTrail" then
                        child.Color = templateTrail.Color
                        child.LightEmission = templateTrail.LightEmission
                        child.Transparency = templateTrail.Transparency
                        child.Enabled = true
                    end
                end
            end
        end
    end
end

local function applyTrailToBall(ballPart)
    if not ballPart or not s.TrailChanger.Enabled then return end
    local templateTrail = getTemplateTrail()
    if not templateTrail then return end

    cleanBallTrails(ballPart)

    local attach0 = Instance.new("Attachment")
    attach0.Name = "AuralAttach0"
    attach0.Position = Vector3.new(0, 0.5, 0)
    attach0.Parent = ballPart

    local attach1 = Instance.new("Attachment")
    attach1.Name = "AuralAttach1"
    attach1.Position = Vector3.new(0, -0.5, 0)
    attach1.Parent = ballPart

    local newTrail = templateTrail:Clone()
    newTrail.Name = "AuralTrail"
    newTrail.Attachment0 = attach0
    newTrail.Attachment1 = attach1
    newTrail.Enabled = true
    newTrail.Parent = ballPart

    ballPart:GetPropertyChangedSignal("BrickColor"):Connect(function()
        task.wait(0.05)
        restoreTrailColor(ballPart)
    end)

    ballPart:GetPropertyChangedSignal("Color"):Connect(function()
        task.wait(0.05)
        restoreTrailColor(ballPart)
    end)
end

local function resetTrailOnBall(ballPart)
    if not ballPart then return end
    cleanBallTrails(ballPart)
end

local function applyTrailToAllBalls()
    for _, desc in workspace:GetDescendants() do
        if desc:IsA("BasePart") and desc.Name == "Ball" then
            applyTrailToBall(desc)
        end
    end
end

local function resetTrailOnAllBalls()
    for _, desc in workspace:GetDescendants() do
        if desc:IsA("BasePart") and desc.Name == "Ball" then
            resetTrailOnBall(desc)
        end
    end
end

local function applyGoalEffect()
    pcall(function()
        local PlayerData = require(ReplicatedStorage.Util.PlayerData)
        PlayerData.storedData["Equipping"]["Effect"] = Effects[s.Goal.CurrentEffect].Name
        PlayerData.storedData["Inventory"][Effects[s.Goal.CurrentEffect].Name] = 1
    end)
    if goalEventHook then return end
    pcall(function()
        local connections = getconnections(ReplicatedStorage.Remotes.goalEvent.OnClientEvent)
        if connections and connections[1] then
            local old
            old = hookfunction(connections[1].Function, function(Player, Court, Goal, XYZ, GoalEffect, Unknown)
                if Player == Client and s.Goal.Enabled then GoalEffect = Effects[s.Goal.CurrentEffect].Value end
                return old(Player, Court, Goal, XYZ, GoalEffect, Unknown)
            end)
            goalEventHook = old
        end
    end)
end

local function resetGoalEffect()
    pcall(function()
        local PlayerData = require(ReplicatedStorage.Util.PlayerData)
        PlayerData.storedData["Equipping"]["Effect"] = Effects["LightRay"].Name
        PlayerData.storedData["Inventory"][Effects["LightRay"].Name] = 1
    end)
end

local oobOriginals = {}
local function setoob(value)
    local found = {}
    for _, object in workspace:GetDescendants() do
        if object:IsA("Folder") and object.Name == "OOB" then
            for _, part in object:GetDescendants() do
                if part:IsA("BasePart") then
                    found[part] = true
                    if not oobOriginals[part] then
                        oobOriginals[part] = {cancollide = part.CanCollide, cantouch = part.CanTouch}
                    end
                    if value then
                        if part.Parent and part.Parent.Name == "Walls" then part.CanCollide = false
                        elseif part.Parent and part.Parent.Name == "Floor" then part.CanTouch = false end
                    end
                end
            end
        end
    end
    for part, original in next, oobOriginals do
        if not found[part] then
            if part and part.Parent then part.CanCollide = original.cancollide part.CanTouch = original.cantouch end
            oobOriginals[part] = nil
        elseif not value and part.Parent then
            part.CanCollide = original.cancollide part.CanTouch = original.cantouch
        end
    end
end

workspace.DescendantAdded:Connect(function(object)
    if not getgenv().Settings.Misc.AntiOOB then return end
    if object:IsA("BasePart") then
        local parent = object.Parent
        local oob = parent and parent.Parent
        if oob and oob:IsA("Folder") and oob.Name == "OOB" then
            oobOriginals[object] = {cancollide = object.CanCollide, cantouch = object.CanTouch}
            if parent.Name == "Walls" then object.CanCollide = false
            elseif parent.Name == "Floor" then object.CanTouch = false end
        end
    end
end)

local function applyBallChanger()
    if not Replicator or not OriginalReplicateBall then return end
    Replicator["ReplicateBall"] = function(Parent, Name)
        Name = Balls[s.BallChanger.CurrentBall]
        return OriginalReplicateBall(Parent, Name)
    end
end

local function resetBallChanger()
    if not Replicator or not OriginalReplicateBall then return end
    Replicator["ReplicateBall"] = OriginalReplicateBall
end

local function CleanCharacter()
    local char = LocalPlayer.Character
    if not char then return end
    local realHead = char:FindFirstChild("Head")
    for _, child in char:GetChildren() do
        if child:IsA("Part") or child:IsA("MeshPart") then
            if child.Name:lower():find("head") and child ~= realHead then child.Transparency = 1 child.CanCollide = false end
            if child.Name:lower() == "aura" then child.Transparency = 1 child.CanCollide = false end
        end
    end
end

local function FindRig(auraModel)
    for _, child in auraModel:GetChildren() do
        if child:IsA("Model") and child:FindFirstChildOfClass("Humanoid") then return child end
    end
    if auraModel:FindFirstChildOfClass("Humanoid") then return auraModel end
    return nil
end

local function GetAuraRigParts(rigModel)
    local parts = {}
    local partNames = {"Head","LeftArm","RightArm","Torso","LeftLeg","RightLeg","HumanoidRootPart"}
    for _, name in partNames do
        local part = rigModel:FindFirstChild(name)
        if part and part:IsA("BasePart") then parts[name] = part end
    end
    return parts
end

local function GetCharacterParts()
    local char = LocalPlayer.Character
    if not char then return {} end
    local parts = {}
    local partNames = {"Head","LeftArm","RightArm","Torso","UpperTorso","LeftLeg","RightLeg","HumanoidRootPart"}
    for _, name in partNames do
        local part = char:FindFirstChild(name)
        if part and part:IsA("BasePart") then parts[name] = part end
    end
    return parts
end

local function RemoveFaceTextures(part)
    if not part then return end
    for _, child in part:GetChildren() do
        if child:IsA("Decal") or child:IsA("Texture") or child:IsA("SurfaceAppearance") then child:Destroy() end
    end
end

local function CreateAuraVisuals(auraModel)
    local char = LocalPlayer.Character
    if not char or not char.Parent then return false end
    if char:FindFirstChild("AuraVisuals") then char.AuraVisuals:Destroy() end
    CleanCharacter()
    local rig = FindRig(auraModel)
    if not rig then return false end
    local auraParts = GetAuraRigParts(rig)
    if not auraParts or next(auraParts) == nil then return false end
    local charParts = GetCharacterParts()
    if not charParts or next(charParts) == nil then return false end
    local container = Instance.new("Model")
    container.Name = "AuraVisuals"
    container.Parent = char
    local charHead = charParts["Head"]
    if not charHead then return false end
    for partName, auraPart in auraParts do
        local charPart = charParts[partName]
        if charPart and auraPart then
            local newPart = auraPart:Clone()
            newPart.Anchored = false
            newPart.CanCollide = false
            newPart.Transparency = 1
            newPart.Parent = container
            if partName == "Head" then
                RemoveFaceTextures(newPart)
                local weld = Instance.new("Weld")
                weld.Part0 = charPart
                weld.Part1 = newPart
                weld.C0 = CFrame.new(0, 0, 0)
                weld.Parent = newPart
            elseif partName == "HumanoidRootPart" then
                newPart.Transparency = 1
                newPart.CanCollide = false
                newPart.Anchored = false
            else
                local weld = Instance.new("Weld")
                weld.Part0 = charPart
                weld.Part1 = newPart
                local referencePart = rig:FindFirstChild("HumanoidRootPart") or rig.PrimaryPart or auraPart
                weld.C0 = CFrame.new(auraPart.Position - referencePart.Position)
                weld.Parent = newPart
            end
        end
    end
    for _, child in container:GetDescendants() do
        if child:IsA("Script") or child:IsA("LocalScript") or child:IsA("ModuleScript") then child:Destroy() end
    end
    return true
end

local function ApplyAura(auraModel)
    local char = LocalPlayer.Character
    if not char then return false end
    if char:FindFirstChild("AuraVisuals") then char.AuraVisuals:Destroy() end
    task.wait(0.1)
    return CreateAuraVisuals(auraModel)
end

local function RemoveAura()
    local char = LocalPlayer.Character
    if char and char:FindFirstChild("AuraVisuals") then char.AuraVisuals:Destroy() end
    return true
end

local hiddenTags = {}
local hideTagsConnection = nil

local function startHideTags()
    if hideTagsConnection then return end
    local function hideNameTag(character)
        local hrp = character:FindFirstChild("HumanoidRootPart")
        if hrp then
            local tag = hrp:FindFirstChild("NameTag")
            if tag then tag.Enabled = false table.insert(hiddenTags, tag) end
            hrp.ChildAdded:Connect(function(child)
                if child.Name == "NameTag" then child.Enabled = false table.insert(hiddenTags, child) end
            end)
        end
    end
    for _, p in Players:GetPlayers() do
        if p.Character then hideNameTag(p.Character) end
        p.CharacterAdded:Connect(hideNameTag)
    end
    Players.PlayerAdded:Connect(function(p) p.CharacterAdded:Connect(hideNameTag) end)
    hideTagsConnection = true
end

local function stopHideTags()
    for _, tag in hiddenTags do
        if tag and tag.Parent then tag.Enabled = true end
    end
    hiddenTags = {}
    hideTagsConnection = nil
end

local isMobile = UserInputService.TouchEnabled

pcall(function()
    local area = workspace:WaitForChild("PracticeArea")
    local courts = workspace:FindFirstChild("Courts")
    area.Parent = courts
end)

local env = getgenv()
if type(env.__standalone_silent_aim_unload) == "function" then
    pcall(env.__standalone_silent_aim_unload)
end

local Gui = Instance.new("ScreenGui")
Gui.Name = "AuralMobileGui"
Gui.ResetOnSpawn = false
Gui.Parent = gethui()

local Button = Instance.new("TextButton")
Button.Name = "AutoGuard"
Button.Size = UDim2.new(0, 55, 0, 55)
Button.Position = UDim2.new(0.8, 0, 0.5, 0)
Button.BackgroundColor3 = Color3.fromRGB(35, 35, 35)
Button.Text = "AG"
Button.TextColor3 = Color3.fromRGB(255, 255, 255)
Button.TextSize = 18
Button.Font = Enum.Font.GothamBold
Button.AutoButtonColor = true
Button.Visible = false
Button.Parent = Gui
Instance.new("UICorner", Button).CornerRadius = UDim.new(1, 0)

local dragging, dragStart, startPos, moved = false, nil, nil, false

Button.InputBegan:Connect(function(input)
    if input.UserInputType == Enum.UserInputType.MouseButton1 or input.UserInputType == Enum.UserInputType.Touch then
        dragging = true
        moved = false
        dragStart = input.Position
        startPos = Button.Position
        input.Changed:Connect(function()
            if input.UserInputState == Enum.UserInputState.End then
                dragging = false
                if not moved then
                    s.Defending.AutoGuard = not s.Defending.AutoGuard
                    Button.BackgroundColor3 = s.Defending.AutoGuard and Color3.fromRGB(0, 170, 85) or Color3.fromRGB(35, 35, 35)
                end
            end
        end)
    end
end)

UserInputService.InputChanged:Connect(function(input)
    if dragging and (input.UserInputType == Enum.UserInputType.MouseMovement or input.UserInputType == Enum.UserInputType.Touch) then
        local delta = input.Position - dragStart
        if delta.Magnitude > 5 then moved = true end
        Button.Position = UDim2.new(startPos.X.Scale, startPos.X.Offset + delta.X, startPos.Y.Scale, startPos.Y.Offset + delta.Y)
    end
end)

local goals = {}

local function validgoal(object)
    if not object:IsA("BasePart") then return false end
    local name = string.lower(object.Name)
    if name ~= "goal" and name ~= "goalhome" and name ~= "goalaway" then return false end
    local parent = object.Parent
    for _ = 1, 5 do
        if not parent then break end
        local parentname = string.lower(parent.Name)
        if string.find(parentname, "hitbox", 1, true) or string.find(parentname, "fieldgoal", 1, true) then return true end
        parent = parent.Parent
    end
    return false
end

local function refreshgoals()
    table.clear(goals)
    for _, object in workspace:GetDescendants() do
        if validgoal(object) then table.insert(goals, object) end
    end
end

refreshgoals()

workspace.DescendantAdded:Connect(function(object)
    if validgoal(object) then table.insert(goals, object) end
end)

workspace.DescendantRemoving:Connect(function(object)
    if not object:IsA("BasePart") then return end
    for i = #goals, 1, -1 do
        if goals[i] == object then table.remove(goals, i) break end
    end
end)

local balls = {}

for _, v in workspace:GetDescendants() do
    if v:IsA("BasePart") and v.Name == "Ball" then table.insert(balls, v) end
end

workspace.DescendantAdded:Connect(function(child)
    if child:IsA("BasePart") and child.Name == "Ball" then table.insert(balls, child) end
end)

workspace.DescendantRemoving:Connect(function(child)
    if not child:IsA("BasePart") or child.Name ~= "Ball" then return end
    for i = #balls, 1, -1 do
        if balls[i] == child then table.remove(balls, i) break end
    end
end)

local function hasball(char)
    return char and char:FindFirstChild("Basketball") ~= nil
end

local function nearestgoal(root)
    local best, bestDist = nil, math.huge
    for i = #goals, 1, -1 do
        local goal = goals[i]
        if not goal or not goal.Parent then
            table.remove(goals, i)
        else
            local delta = goal.Position - root.Position
            local flat = Vector3.new(delta.X, 0, delta.Z).Magnitude
            if flat < bestDist then best = goal bestDist = flat end
        end
    end
    return best, bestDist
end

local function getclosestball()
    local closest, closestDist = nil, math.huge
    if not HRP then return nil, math.huge end
    for _, ball in balls do
        local d = (HRP.Position - ball.Position).Magnitude
        if d < closestDist then closestDist = d closest = ball end
    end
    return closest, closestDist
end

local function getclosestplayerwithball()
    local closest, closestDist = nil, math.huge
    if not HRP then return nil, math.huge end
    for _, player in Players:GetPlayers() do
        if player ~= LocalPlayer and player.Character then
            local basketball = player.Character:FindFirstChild("Basketball")
            if basketball and basketball:FindFirstChild("Ball") then
                local hrp2 = player.Character:FindFirstChild("HumanoidRootPart")
                if hrp2 then
                    local d = (HRP.Position - hrp2.Position).Magnitude
                    if d < closestDist then closestDist = d closest = player end
                end
            end
        end
    end
    return closest, closestDist
end

local state = {
    target = nil, connections = {}, actorst = nil, actorname = nil,
    actors = {}, oldnewindex = nil, active = true, clicking = false
}

local function addconnection(c) table.insert(state.connections, c) return c end

local function settarget(target)
    state.target = target
    if state.actorst then pcall(function() state.actorst.t = target end) end
end

local function doClick()
    if state.clicking then return end
    state.clicking = true
    if isMobile then
        if Icon then
            local pos = Vector2.new(Icon.AbsolutePosition.X + Icon.AbsoluteSize.X / 2, Icon.AbsolutePosition.Y + Icon.AbsoluteSize.Y / 2)
            Input:SendMouseButtonEvent(pos.X, pos.Y, 0, true, game, 1)
            Input:SendMouseButtonEvent(pos.X, pos.Y, 0, false, game, 1)
        end
    else
        local vp = Camera.ViewportSize
        Input:SendMouseButtonEvent(vp.X / 2, vp.Y / 2, 0, true, game, 1)
        Input:SendMouseButtonEvent(vp.X / 2, vp.Y / 2, 0, false, game, 1)
    end
    task.delay(0.35, function() state.clicking = false end)
end

local function updateAuralTarget()
    if not state.active or not s.Shooting.AuralEnabled then settarget(nil) return end
    local char = LocalPlayer.Character
    local hum = char and char:FindFirstChildOfClass("Humanoid")
    local root = char and char:FindFirstChild("HumanoidRootPart")
    local head = char and char:FindFirstChild("Head")
    if not char or not hum or not root or not head or not hasball(char) then settarget(nil) return end
    if hum.FloorMaterial ~= Enum.Material.Air and not hum.Jump then settarget(nil) return end
    local goal, distance = nearestgoal(root)
    if not goal then settarget(nil) return end
    local offset = getArcOffset(distance) + getgenv().Settings.Shooting.ShotYAxis
    settarget(goal.Position + Vector3.new(0, offset, 0))
end

local actorcode = [==[
local registry = game:GetService("SharedTableRegistry")
local players = game:GetService("Players")
local st = registry:GetSharedTable(__silentaimtable)
local camera = workspace.CurrentCamera
local function target() local v = st.t return typeof(v) == "Vector3" and v or nil end
local function aimcframe()
    local v = target()
    local char = players.LocalPlayer.Character
    local head = char and char:FindFirstChild("Head")
    if v and head then return CFrame.new(head.Position, v) end
end
pcall(function()
    local oldrender
    oldrender = hookfunction(camera.GetRenderCFrame, function(self, ...) return aimcframe() or oldrender(self, ...) end)
    local oldscreen
    oldscreen = hookfunction(camera.ScreenPointToRay, function(self, ...)
        local v = aimcframe()
        if v then return Ray.new(v.Position, v.LookVector) end
        return oldscreen(self, ...)
    end)
    local oldviewport
    oldviewport = hookfunction(camera.ViewportPointToRay, function(self, ...)
        local v = aimcframe()
        if v then return Ray.new(v.Position, v.LookVector) end
        return oldviewport(self, ...)
    end)
    local cameradata = registry:GetSharedTable("CameraData")
    local history = cameradata.CameraHistory
    local meta = getrawmetatable(cameradata)
    local oldnewindex
    oldnewindex = hookfunction(meta.__newindex, newcclosure(function(tab, key, value)
        local current = tab == cameradata and key == "CameraCFrame"
        local previous = history ~= nil and tab == history and type(key) == "number"
        local aimed = aimcframe()
        if aimed and (current or previous) then return oldnewindex(tab, key, aimed) end
        return oldnewindex(tab, key, value)
    end))
end)
]==]

local function hookmain()
    pcall(function()
        state.actorname = "sa" .. tostring(math.floor(os.clock() * 100000)) .. tostring(math.random(10000, 99999))
        state.actorst = SharedTableRegistry:GetSharedTable(state.actorname)
        state.actorst.t = nil
        local cameradata = SharedTableRegistry:GetSharedTable("CameraData")
        local history = cameradata.CameraHistory
        local meta = getrawmetatable(cameradata)
        state.oldnewindex = hookfunction(meta.__newindex, newcclosure(function(tab, key, value)
            local current = tab == cameradata and key == "CameraCFrame"
            local previous = history ~= nil and tab == history and type(key) == "number"
            local tgt = state.target
            if tgt and (current or previous) and typeof(value) == "CFrame" then
                local char = LocalPlayer.Character
                local head = char and char:FindFirstChild("Head")
                if head then return state.oldnewindex(tab, key, CFrame.new(head.Position, tgt)) end
            end
            return state.oldnewindex(tab, key, value)
        end))
    end)
end

local function hookactors()
    if type(getactors) ~= "function" or type(run_on_actor) ~= "function" or not state.actorname then return end
    local ok, actors = pcall(getactors)
    if not ok or type(actors) ~= "table" then return end
    local code = "local __silentaimtable = " .. string.format("%q", state.actorname) .. " " .. actorcode
    for _, actor in actors do
        if not state.actors[actor] then
            if pcall(run_on_actor, actor, code) then state.actors[actor] = true end
        end
    end
end

local function setupCharacterAural(char)
    local hum = char:WaitForChild("Humanoid", 5)
    if not hum then return end
    addconnection(hum.Jumping:Connect(function()
        if not s.Shooting.AuralEnabled then return end
        if not hasball(char) then return end
        local root = char:FindFirstChild("HumanoidRootPart")
        if not root then return end
        local goal, distance = nearestgoal(root)
        local minDist = isMobile and 30 or 12
        if not goal or distance < minDist or distance > 73 then return end
        local arc = getArcOffset(distance) + s.Shooting.ShotYAxis
        local targetPos = goal.Position + Vector3.new(0, arc, 0)
        settarget(targetPos)
        task.wait(0.05)
        LocalPlayer:SetAttribute("Power", 85)
        local shotdelay = s.Shooting.ShotDelay or 0.25
        if s.Shooting.RandomizeShotDelay then
            local mn = s.Shooting.ShotDelayMin or 0.15
            local mx = s.Shooting.ShotDelayMax or 0.35
            if mx < mn then mx = mn end
            shotdelay = mn + math.random() * (mx - mn)
        end
        task.wait(shotdelay)
        doClick()
        task.wait(0.1)
        updateAuralTarget()
    end))
end

if LocalPlayer.Character then setupCharacterAural(LocalPlayer.Character) end
addconnection(LocalPlayer.CharacterAdded:Connect(setupCharacterAural))

hookmain()
hookactors()

addconnection(workspace.DescendantAdded:Connect(function(object)
    if validgoal(object) then table.insert(goals, object) end
end))

addconnection(RunService.Heartbeat:Connect(updateAuralTarget))
addconnection(Players.PlayerAdded:Connect(function() task.defer(hookactors) end))

env.__standalone_silent_aim_settings = {enabled = true, requireball = true}
env.__standalone_silent_aim_unload = function()
    state.active = false
    settarget(nil)
    for _, c in state.connections do pcall(function() c:Disconnect() end) end
    table.clear(state.connections)
    if state.oldnewindex and type(restorefunction) == "function" then pcall(restorefunction, state.oldnewindex) end
    env.__standalone_silent_aim_settings = nil
    env.__standalone_silent_aim_unload = nil
end

local fatalState = {
    shooting = false, cachedrim = nil, cacheddist = math.huge,
    lastjumpshot = 0, groundy = nil, lastvelY = 0, peakfiring = false, renderConnection = nil,
}

local function setpower(p)
    LocalPlayer:SetAttribute("Power", p)
    pcall(function() SharedTableRegistry:GetSharedTable("PowerData").Power = p end)
end

local function screencenter()
    local sc = Camera.ViewportSize
    return sc.X / 2, sc.Y / 2
end

local rimsCache = {}
local function buildrims()
    rimsCache = {}
    for _, desc in workspace:GetDescendants() do
        if desc:IsA("BasePart") and (desc.Name == "Goal" or desc.Name == "Rim") then
            table.insert(rimsCache, desc)
        end
    end
end
buildrims()

local function nearestrim(pos)
    local best, bestd = nil, math.huge
    for _, rim in rimsCache do
        if rim and rim.Parent then
            local d = (rim.Position - pos).Magnitude
            if d < bestd then bestd = d best = rim end
        end
    end
    return best, bestd
end

local function flickshot_fatal()
    if fatalState.shooting then return end
    fatalState.shooting = true
    local char = LocalPlayer.Character
    if not char then fatalState.shooting = false return end
    local hrp = char:FindFirstChild("HumanoidRootPart")
    if not hrp then fatalState.shooting = false return end
    local rim, dist = fatalState.cachedrim, fatalState.cacheddist
    if not rim then fatalState.shooting = false return end
    local selectedPower = 85
    if dist > 73 then selectedPower = 90 if dist > 80 then selectedPower = 95 end end
    local maxDist = 73
    if selectedPower == 90 then maxDist = 80 elseif selectedPower == 95 then maxDist = 85 end
    if dist < 7 or dist > maxDist then fatalState.shooting = false return end
    local arc = getArc(dist, selectedPower)
    if dist < 59 then
        local toRim = (rim.Position - hrp.Position).Unit
        local velocityTowardRim = hrp.AssemblyLinearVelocity:Dot(toRim)
        if velocityTowardRim > 0 then arc = arc - (velocityTowardRim * 0.18) end
        if velocityTowardRim < -1 then arc = arc + 5 end
    end
    local GreenComp = Vector3.new(0, 0, 0)
    local sidewaysComp = Vector3.new(0, 0, 0)
    if math.random(1, 100) <= s.Shooting.GreenChance then
        GreenComp = Vector3.new(0, 4.05, 0)
    else
        GreenComp = Vector3.new(0, -1.14, 0)
        if math.random(0, 1) == 0 then sidewaysComp = Camera.CFrame.RightVector * -0.2
        else sidewaysComp = Camera.CFrame.RightVector * 0.2 end
    end
    local compensation = hrp.AssemblyLinearVelocity * (dist / 50) * 0.05
    local aimTarget = rim.Position + Vector3.new(0, arc, 0) + Vector3.new(0, 0.95, 0) + GreenComp + compensation + sidewaysComp
    local origcf = Camera.CFrame
    local origct = Camera.CameraType
    local origmb = UserInputService.MouseBehavior
    setpower(selectedPower)
    local cx, cy = screencenter()
    Camera.CameraType = Enum.CameraType.Scriptable
    UserInputService.MouseBehavior = Enum.MouseBehavior.LockCenter
    Camera.CFrame = CFrame.lookAt(hrp.Position, aimTarget)
    Input:SendMouseButtonEvent(cx, cy, 0, true, game, 1)
    RunService.RenderStepped:Wait()
    Input:SendMouseButtonEvent(cx, cy, 0, false, game, 1)
    Camera.CFrame = origcf
    Camera.CameraType = origct
    UserInputService.MouseBehavior = origmb
    fatalState.shooting = false
end

local function fatalRenderLoop()
    if not s.Shooting.FatalEnabled then return end
    local char = LocalPlayer.Character
    local hrp = char and char:FindFirstChild("HumanoidRootPart")
    if not hrp then fatalState.cachedrim, fatalState.cacheddist = nil, math.huge fatalState.lastvelY = 0 return end
    local humanoid = char:FindFirstChildOfClass("Humanoid")
    local airborne = humanoid and (humanoid:GetState() == Enum.HumanoidStateType.Freefall or humanoid:GetState() == Enum.HumanoidStateType.Jumping)
    if not airborne then fatalState.groundy = hrp.Position.Y end
    local rim, dist = nearestrim(hrp.Position)
    fatalState.cachedrim, fatalState.cacheddist = rim, dist
    if not rim then fatalState.lastvelY = hrp.AssemblyLinearVelocity.Y return end
    if s.Shooting.ThreesOnly and dist < 55 then fatalState.lastvelY = hrp.AssemblyLinearVelocity.Y return end
    local velY = hrp.AssemblyLinearVelocity.Y
    if not fatalState.shooting then
        if airborne and fatalState.lastvelY > 0 and velY <= 0 and not fatalState.peakfiring and hasball(char) and (os.clock() - fatalState.lastjumpshot) > 0.5 then
            if s.Shooting.AimAssist then
                local lookY = Camera.CFrame.LookVector.Y
                if lookY <= 0.3 then fatalState.lastvelY = velY return end
            end
            fatalState.peakfiring = true
            fatalState.lastjumpshot = os.clock()
            task.spawn(function() flickshot_fatal() fatalState.peakfiring = false end)
        end
        if not airborne then fatalState.peakfiring = false end
    end
    fatalState.lastvelY = velY
end

local function enableFatal()
    if fatalState.renderConnection then return end
    fatalState.renderConnection = RunService.RenderStepped:Connect(fatalRenderLoop)
end

local function disableFatal()
    if fatalState.renderConnection then fatalState.renderConnection:Disconnect() fatalState.renderConnection = nil end
    fatalState.shooting = false
end

local guardConnection = nil
local speedConnection = nil

local function enableAutoGuard()
    if guardConnection then guardConnection:Disconnect() guardConnection = nil end
    local frameSkip = 0
    guardConnection = RunService.Stepped:Connect(function()
        if not s.Defending.AutoGuard then return end
        frameSkip += 1
        if frameSkip < 3 then return end
        frameSkip = 0
        local char = LocalPlayer.Character
        local myHum = char and char:FindFirstChild("Humanoid")
        if not myHum or hasball(char) then return end
        local dribbler = getClosestDribbler()
        if dribbler then
            local guardPos = getGuardPosition(dribbler)
            if guardPos then myHum:MoveTo(guardPos) end
            return
        end
        if s.Defending.AutoGuardNearest then
            local nearest = getClosestPlayer()
            if nearest then
                local guardPos = getGuardPosition(nearest)
                if guardPos then myHum:MoveTo(guardPos) end
            end
        end
    end)
end

local function disableAutoGuard()
    if guardConnection then guardConnection:Disconnect() guardConnection = nil end
end

local function enableSpeedBoost()


    if speedConnection then speedConnection:Disconnect() speedConnection = nil end





    speedConnection = RunService.Heartbeat:Connect(function(dt)


        if not s.Player.Walkspeed then return end





        local char = LocalPlayer.Character


        local hrp = char and char:FindFirstChild("HumanoidRootPart")


        local humanoid = char and char:FindFirstChildOfClass("Humanoid")





        if not hrp or not humanoid then return end





        local moveDir = humanoid.MoveDirection





        if moveDir.Magnitude > 0 then


            local camCF = Camera.CFrame


            local forward = Vector3.new(camCF.LookVector.X, 0, camCF.LookVector.Z)


            local right = Vector3.new(camCF.RightVector.X, 0, camCF.RightVector.Z)





            forward = forward.Magnitude > 0 and forward.Unit or Vector3.zero


            right = right.Magnitude > 0 and right.Unit or Vector3.zero





            local x = moveDir:Dot(right)


            local z = moveDir:Dot(forward)


            moveDir = (right * x + forward * z)





            if moveDir.Magnitude > 0 then


                moveDir = moveDir.Unit


                local extraSpeed = 16 * s.Player.WSvalue - 16


                hrp.CFrame = hrp.CFrame + moveDir * extraSpeed * dt


            end


        end


    end)


end





local function disableSpeedBoost()


    if speedConnection then speedConnection:Disconnect() speedConnection = nil end


end

if s.Defending.AutoGuard then enableAutoGuard() end
if s.Player.Walkspeed then enableSpeedBoost() end
if s.Ball.BallMags then enableMagnet() end

UserInputService.InputBegan:Connect(function(input, gpe)
    if gpe then return end
    local magKey = s.Ball.MagnetKeybind
    if magKey and input.KeyCode == Enum.KeyCode[magKey] then
        s.Ball.BallMags = not s.Ball.BallMags
        if s.Ball.BallMags then enableMagnet() else disableMagnet() end
        pcall(function()
            if uiToggles and uiToggles.magnet then uiToggles.magnet:Set(s.Ball.BallMags) end
        end)
        return
    end
end)

local function SpoofDevice()
    if not s.Misc.DeviceSpoofer then return end
    local device = s.Misc.Device
    if device == "PC" then DeviceEvent:FireServer("Desktop")
    elseif device == "Mobile" then DeviceEvent:FireServer("Mobile")
    elseif device == "Gamepad" then DeviceEvent:FireServer("Gamepad") end
end

local function beamProjectile(g, v0, x0, t1)
    local c = 0.125
    local p3 = 0.5 * g * t1 * t1 + v0 * t1 + x0
    local p2 = p3 - (g * t1 * t1 + v0 * t1) / 3
    local p1 = (c * g * t1 * t1 + 0.5 * v0 * t1 + x0 - c * (x0 + p3)) / (3 * c) - p2
    local curve0 = (p1 - x0).Magnitude
    local curve1 = (p2 - p3).Magnitude
    local b = (x0 - p3).Unit
    local r1 = (p1 - x0).Unit
    local u1 = r1:Cross(b).Unit
    local r2 = (p2 - p3).Unit
    local u2 = r2:Cross(b).Unit
    b = u1:Cross(r1).Unit
    local cf1 = CFrame.new(x0.x, x0.y, x0.z, r1.x, u1.x, b.x, r1.y, u1.y, b.y, r1.z, u1.z, b.z)
    local cf2 = CFrame.new(p3.x, p3.y, p3.z, r2.x, u2.x, b.x, r2.y, u2.y, b.y, r2.z, u2.z, b.z)
    return curve0, -curve1, cf1, cf2
end

workspace.ChildAdded:Connect(function(b)
    if b.Name == "Basketball" and getgenv().Settings.Visuals.VisualizeBallProjectory then
        local ball = b:FindFirstChild("Ball")
        if ball and ball:IsA("BasePart") then
            task.wait()
            local vel = ball.AssemblyLinearVelocity
            local pos = ball.Position
            local g = Vector3.new(0, -workspace.Gravity, 0)
            local c0, c1, cf1, cf2 = beamProjectile(g, vel, pos, 10)
            local beam = Instance.new("Beam")
            local a0 = Instance.new("Attachment")
            local a1 = Instance.new("Attachment")
            beam.Transparency = NumberSequence.new({
                NumberSequenceKeypoint.new(0, 1),
                NumberSequenceKeypoint.new(0.01, 0),
                NumberSequenceKeypoint.new(1, 0),
                NumberSequenceKeypoint.new(1, 0.01)
            })
            beam.CurveSize0 = c0
            beam.CurveSize1 = c1
            beam.Segments = 1750
            beam.Width0 = 1
            beam.Width1 = 1
            beam.Name = "Hitbox"
            beam.Parent = workspace.Terrain
            a0.Parent = workspace.Terrain
            a1.Parent = workspace.Terrain
            a0.CFrame = a0.Parent.CFrame:Inverse() * cf1
            a1.CFrame = a0.Parent.CFrame:Inverse() * cf2
            beam.Attachment0 = a0
            beam.Attachment1 = a1
            local alive = true
            local anim
            local chosen = TrajColors[s.Visuals.ProjectoryColor]
            if chosen then
                beam.Color = ColorSequence.new(chosen)
            else
                local t = 0
                anim = RunService.Heartbeat:Connect(function(dt)
                    if not alive then return end
                    t += dt
                    local hue = (t * 0.25) % 1
                    beam.Color = ColorSequence.new({
                        ColorSequenceKeypoint.new(0, Color3.fromHSV(hue, 1, 1)),
                        ColorSequenceKeypoint.new(1, Color3.fromHSV((hue + 0.15) % 1, 1, 1))
                    })
                end)
            end
            local function cleanup()
                if not alive then return end
                alive = false
                if anim then anim:Disconnect() end
                if landedConnection then landedConnection:Disconnect() end
                beam:Destroy() a0:Destroy() a1:Destroy()
            end
            local rayParams = RaycastParams.new()
            rayParams.FilterType = Enum.RaycastFilterType.Exclude
            rayParams.FilterDescendantsInstances = { b }
            landedConnection = RunService.Heartbeat:Connect(function()
                if not alive then landedConnection:Disconnect() return end
                if not ball or ball.Parent == nil or ball.Parent ~= b then cleanup() return end
                local result = workspace:Raycast(ball.Position, Vector3.new(0, -3, 0), rayParams)
                if result and (ball.Position.Y - result.Position.Y) <= 2.5 then cleanup() end
            end)
            task.spawn(function()
                repeat task.wait() until not alive or not ball or ball.Parent ~= b
                if alive then cleanup() end
            end)
        end
    end

    if b.Name == "Basketball" then
        local ball = b:FindFirstChild("Ball")
        if ball and s.TrailChanger.Enabled then
            task.wait(0.05)
            applyTrailToBall(ball)
        end
    end
end)

if s.TrailChanger.Enabled then
    task.delay(0.5, applyTrailToAllBalls)
end

local zolar = loadstring(game:HttpGet("https://raw.githubusercontent.com/wimazawa-eng/ap/refs/heads/main/Testing.lua.txt"))()

local window = zolar:Window({
    Name = "Aural Hub",
    Icon = "105577669304412",
    Accent = Color3.fromRGB(179, 165, 255)
})

local uiToggles = {}

local offenseTab = window:Tab({ Name = "Offense", Icon = "swords" })
local offenseSub = offenseTab:SubTab({ Name = "Silent Aim", Icon = "crosshair" })

local auralSection = offenseSub:Section({ Name = "Silent Aim (Aural)", Side = 1 })
auralSection:Toggle({
    Name = "Enable Aural Silent Aim",
    Default = s.Shooting.AuralEnabled,
    Flag = "aural_toggle",
    Callback = function(value)
        s.Shooting.AuralEnabled = value
        if value then s.Shooting.FatalEnabled = false s.Shooting.Mode = "Aural" disableFatal() end
    end
})
auralSection:Slider({
    Name = "Shot Y axis offset",
    Min = -10, Max = 10, Default = s.Shooting.ShotYAxis, Decimals = 0.1,
    Flag = "aural_yaxis",
    Callback = function(value) s.Shooting.ShotYAxis = value end
})
auralSection:Slider({
    Name = "Shot delay",
    Min = 0.15, Max = 0.42, Default = s.Shooting.ShotDelay, Decimals = 0.01, Suffix = "s",
    Flag = "aural_delay",
    Callback = function(value) s.Shooting.ShotDelay = value end
})
auralSection:Toggle({
    Name = "Randomize shot delay",
    Default = s.Shooting.RandomizeShotDelay,
    Flag = "aural_rand",
    Callback = function(value) s.Shooting.RandomizeShotDelay = value end
})
auralSection:Slider({
    Name = "Min delay",
    Min = 0, Max = 1, Default = s.Shooting.ShotDelayMin, Decimals = 0.05, Suffix = "s",
    Flag = "aural_mindelay",
    Callback = function(value) s.Shooting.ShotDelayMin = value end
})
auralSection:Slider({
    Name = "Max delay",
    Min = 0, Max = 1, Default = s.Shooting.ShotDelayMax, Decimals = 0.05, Suffix = "s",
    Flag = "aural_maxdelay",
    Callback = function(value) s.Shooting.ShotDelayMax = value end
})

local fatalSection = offenseSub:Section({ Name = "Silent Aim 2", Side = 2 })
fatalSection:Toggle({
    Name = "Enable Fatal Silent Aim",
    Default = s.Shooting.FatalEnabled,
    Flag = "fatal_toggle",
    Callback = function(value)
        s.Shooting.FatalEnabled = value
        if value then s.Shooting.AuralEnabled = false s.Shooting.Mode = "Fatal" enableFatal()
        else disableFatal() end
    end
})
fatalSection:Slider({
    Name = "Green Chance",
    Min = 0, Max = 100, Default = s.Shooting.GreenChance, Step = 5, Suffix = "%",
    Flag = "fatal_green",
    Callback = function(value) s.Shooting.GreenChance = value end
})
fatalSection:Toggle({
    Name = "High Arc (85s only)",
    Default = s.Shooting.ThreesOnly,
    Flag = "fatal_threes",
    Callback = function(value) s.Shooting.ThreesOnly = value end
})
fatalSection:Toggle({
    Name = "Aim Assist (only shoot when looking up)",
    Default = s.Shooting.AimAssist,
    Flag = "fatal_aimassist",
    Callback = function(value) s.Shooting.AimAssist = value end
})

local defenseTab = window:Tab({ Name = "Defense", Icon = "shield" })
local defenseSub = defenseTab:SubTab({ Name = "Defense", Icon = "shield" })

local guardSec = defenseSub:Section({ Name = "Auto Guard", Side = 1 })
local guardToggle = guardSec:Toggle({
    Name = "Auto guard",
    Default = s.Defending.AutoGuard,
    Flag = "defend_autoguard",
    Callback = function(value)
        s.Defending.AutoGuard = value
        if value then enableAutoGuard() else disableAutoGuard() end
    end
})
guardToggle:Keybind({
    Name = "Guard keybind",
    Default = Enum.KeyCode.K,
    Flag = "defend_guardkey"
})
guardSec:Toggle({
    Name = "Guard nearest if no dribbler",
    Default = s.Defending.AutoGuardNearest,
    Flag = "defend_nearest",
    Callback = function(value) s.Defending.AutoGuardNearest = value end
})
guardSec:Slider({
    Name = "Guard range",
    Min = 10, Max = 60, Default = s.Defending.AutoGuardRange,
    Flag = "defend_range",
    Callback = function(value) s.Defending.AutoGuardRange = value end
})
guardSec:Slider({
    Name = "Prediction strength",
    Min = 0.1, Max = 1.0, Default = s.Defending.GuardPrediction, Decimals = 0.05,
    Flag = "defend_prediction",
    Callback = function(value) s.Defending.GuardPrediction = value end
})

local ballSec = defenseSub:Section({ Name = "Ball Magnet", Side = 2 })
local magnetToggle = ballSec:Toggle({
    Name = "Ball magnet",
    Default = s.Ball.BallMags,
    Flag = "ball_mags",
    Callback = function(value)
        s.Ball.BallMags = value
        if value then enableMagnet() else disableMagnet() end
    end
})
uiToggles.magnet = magnetToggle
ballSec:Keybind({
    Name = "Magnet keybind",
    Default = Enum.KeyCode.M,
    Flag = "ball_mag_keybind",
    Callback = function(key) s.Ball.MagnetKeybind = key.Name end
})
ballSec:Slider({
    Name = "Magnet size",
    Min = 5, Max = 50, Default = s.Ball.BallMagRange,
    Flag = "ball_magrange",
    Callback = function(value)
        s.Ball.BallMagRange = value
        if magnetHitbox then magnetHitbox.Size = Vector3.new(value, value, value) end
    end
})
ballSec:Toggle({
    Name = "Show visualizer",
    Default = s.Ball.BallMagVisualizer,
    Flag = "ball_visualizer",
    Callback = function(value)
        s.Ball.BallMagVisualizer = value
        if magnetHitbox then magnetHitbox.Transparency = value and 0.65 or 1 end
    end
})

local speedSec = defenseSub:Section({ Name = "Speed Boost", Side = 1 })

local visualizeTab = window:Tab({ Name = "Visualize", Icon = "eye" })
local visualizeSub = visualizeTab:SubTab({ Name = "Visuals", Icon = "eye" })

local goalSec = visualizeSub:Section({ Name = "Goal Effect Changer", Side = 1 })
goalSec:Dropdown({
    Name = "Select effect",
    Items = #EffectNames > 0 and EffectNames or {"None"},
    Default = s.Goal.SelectedEffect or "LightRay",
    Flag = "goal_effect_select",
    Callback = function(value)
        s.Goal.SelectedEffect = value s.Goal.CurrentEffect = value
        if s.Goal.Enabled then pcall(applyGoalEffect) end
    end
})
goalSec:Toggle({
    Name = "Enable goal effect changer",
    Default = s.Goal.Enabled,
    Flag = "goal_effect_toggle",
    Callback = function(value)
        s.Goal.Enabled = value
        if value then pcall(applyGoalEffect) else pcall(resetGoalEffect) end
    end
})
goalSec:Button({
    Name = "Reset to default effect",
    Callback = function()
        s.Goal.Enabled = false s.Goal.SelectedEffect = "LightRay" s.Goal.CurrentEffect = "LightRay"
        pcall(resetGoalEffect)
    end
})

local ballChangerSec = visualizeSub:Section({ Name = "Ball Changer", Side = 2 })
ballChangerSec:Dropdown({
    Name = "Select ball",
    Items = #BallNames > 0 and BallNames or {"None"},
    Default = s.BallChanger.SelectedBall or "ForceField",
    Flag = "ball_changer_select",
    Callback = function(value)
        s.BallChanger.SelectedBall = value s.BallChanger.CurrentBall = value
        if s.BallChanger.Enabled then pcall(applyBallChanger) end
    end
})
ballChangerSec:Toggle({
    Name = "Enable ball changer",
    Default = s.BallChanger.Enabled,
    Flag = "ball_changer_toggle",
    Callback = function(value)
        s.BallChanger.Enabled = value
        if value then pcall(applyBallChanger) else pcall(resetBallChanger) end
    end
})
ballChangerSec:Button({
    Name = "Reset to default ball",
    Callback = function()
        s.BallChanger.Enabled = false s.BallChanger.SelectedBall = "ForceField" s.BallChanger.CurrentBall = "ForceField"
        pcall(resetBallChanger)
    end
})

local trailSec = visualizeSub:Section({ Name = "Ball Trail Changer", Side = 2 })
trailSec:Dropdown({
    Name = "Select trail",
    Items = #TrailNames > 0 and TrailNames or {"None"},
    Default = s.TrailChanger.SelectedTrail or "None",
    Flag = "trail_select",
    Callback = function(value)
        s.TrailChanger.SelectedTrail = value
        s.TrailChanger.CurrentTrail = value
        if s.TrailChanger.Enabled then applyTrailToAllBalls() end
    end
})
local trailToggle = trailSec:Toggle({
    Name = "Enable trail changer",
    Default = s.TrailChanger.Enabled,
    Flag = "trail_toggle",
    Callback = function(value)
        s.TrailChanger.Enabled = value
        if value then applyTrailToAllBalls() else resetTrailOnAllBalls() end
    end
})
uiToggles.trail = trailToggle
trailSec:Button({
    Name = "Reset to default trail",
    Callback = function()
        s.TrailChanger.Enabled = false
        s.TrailChanger.SelectedTrail = "None"
        s.TrailChanger.CurrentTrail = "None"
        resetTrailOnAllBalls()
        if uiToggles.trail then uiToggles.trail:Set(false) end
    end
})

local auraSec = visualizeSub:Section({ Name = "Aura Changer", Side = 1 })
auraSec:Dropdown({
    Name = "Select aura",
    Items = AuraNames,
    Default = s.Aura.SelectedAura or "None",
    Flag = "aura_select",
    Callback = function(value)
        s.Aura.SelectedAura = value
        if s.Aura.Enabled and value ~= "None" then
            for _, aura in AuraList do
                if aura.Name == value then pcall(function() ApplyAura(aura) end) break end
            end
        elseif value == "None" then pcall(RemoveAura) end
    end
})
auraSec:Toggle({
    Name = "Enable aura",
    Default = s.Aura.Enabled,
    Flag = "aura_toggle",
    Callback = function(value)
        s.Aura.Enabled = value
        if value and s.Aura.SelectedAura ~= "None" then
            for _, aura in AuraList do
                if aura.Name == s.Aura.SelectedAura then pcall(function() ApplyAura(aura) end) break end
            end
        else pcall(RemoveAura) end
    end
})
auraSec:Button({
    Name = "Remove aura",
    Callback = function()
        s.Aura.Enabled = false s.Aura.SelectedAura = "None" pcall(RemoveAura)
    end
})

local otherVisSec = visualizeSub:Section({ Name = "Other Visuals", Side = 2 })
otherVisSec:Toggle({
    Name = "Visualize ball trajectory",
    Default = s.Visuals.VisualizeBallProjectory,
    Flag = "vis_trajectory",
    Callback = function(value) s.Visuals.VisualizeBallProjectory = value end
})
otherVisSec:Dropdown({
    Name = "Trajectory color",
    Items = { "Red", "Orange", "Yellow", "Green", "Blue", "Purple", "White", "Rainbow" },
    Default = s.Visuals.ProjectoryColor,
    Flag = "vis_trajcolor",
    Callback = function(value) s.Visuals.ProjectoryColor = value end
})
otherVisSec:Toggle({
    Name = "Aimbot range indicator",
    Default = s.Visuals.ShotIndicator,
    Flag = "vis_shotindicator",
    Callback = function(value)
        s.Visuals.ShotIndicator = value
        if rangeConnection then rangeConnection:Disconnect() rangeConnection = nil end
        indicator.Parent = nil
        if value then
            local rangeThrottle = 0
            rangeConnection = RunService.Heartbeat:Connect(function(dt)
                rangeThrottle += dt
                if rangeThrottle < 0.1 then return end
                rangeThrottle = 0
                local character = LocalPlayer.Character
                local root = character and character:FindFirstChild("HumanoidRootPart")
                if not root then indicator.Parent = nil return end
                local goal, distance = nearestgoal(root)
                indicator.Parent = (goal and distance >= 12 and distance <= 73 and hasball(character)) and character or nil
            end)
        end
    end
})
otherVisSec:Toggle({
    Name = "Hide player tags",
    Default = s.Visuals.HidePlayerTags,
    Flag = "vis_hidetags",
    Callback = function(value)
        s.Visuals.HidePlayerTags = value
        if value then startHideTags() else stopHideTags() end
    end
})
otherVisSec:Toggle({
    Name = "Show hidden ranks",
    Default = s.Visuals.ShowHiddenRanks,
    Flag = "vis_hidden",
    Callback = function(value) s.Visuals.ShowHiddenRanks = value end
})
otherVisSec:Toggle({
    Name = "Include yourself",
    Default = s.Visuals.IncludeYourself,
    Flag = "vis_self",
    Callback = function(value) s.Visuals.IncludeYourself = value end
})

local utilityTab = window:Tab({ Name = "Utility", Icon = "list" })
local utilitySub = utilityTab:SubTab({ Name = "Utility", Icon = "list" })

local autoSec = utilitySub:Section({ Name = "Auto Queue", Side = 1 })
autoSec:Toggle({
    Name = "Auto queue",
    Default = s.Automatics.AutoQueue,
    Flag = "auto_queue",
    Callback = function(value)
        s.Automatics.AutoQueue = value
        if value then startAutoQueue() else stopAutoQueue() end
    end
})
autoSec:Toggle({
    Name = "Queue almost finished",
    Default = s.Automatics.QueueAlmostFinished,
    Flag = "auto_finished",
    Callback = function(value) s.Automatics.QueueAlmostFinished = value end
})
autoSec:Toggle({
    Name = "Requeue after games",
    Default = s.Automatics.AutoQueueAfterGames,
    Flag = "auto_requeue",
    Callback = function(value) s.Automatics.AutoQueueAfterGames = value end
})
autoSec:Dropdown({
    Name = "Join type",
    Items = { "Teleport", "Walk" },
    Default = s.Automatics.JoinType,
    Flag = "auto_jointype",
    Callback = function(value) s.Automatics.JoinType = value end
})

local miscSec = utilitySub:Section({ Name = "Miscellaneous", Side = 2 })
miscSec:Toggle({
    Name = "Anti travel",
    Default = s.Misc.AntiTravel,
    Flag = "misc_travel",
    Callback = function(value)
        s.Misc.AntiTravel = value
        for _, conn in next, getconnections(Humanoid.StateChanged) do
            if conn.Function ~= nil and value then pcall(function() conn:Disable() end)
            else pcall(function() conn:Enable() end) end
        end
    end
})
miscSec:Toggle({
    Name = "Anti out of bounds",
    Default = s.Misc.AntiOOB,
    Flag = "misc_oob",
    Callback = function(value) s.Misc.AntiOOB = value setoob(value) end
})
miscSec:Toggle({
    Name = "Anti moderator",
    Default = s.Misc.AntiModerator,
    Flag = "misc_mod",
    Callback = function(value) s.Misc.AntiModerator = value end
})
miscSec:Dropdown({
    Name = "Mod detection type",
    Items = { "Notify", "Kick" },
    Default = s.Misc.Type,
    Flag = "misc_modtype",
    Callback = function(value) s.Misc.Type = value end
})
miscSec:Toggle({
    Name = "Device spoofer",
    Default = s.Misc.DeviceSpoofer,
    Flag = "misc_devspoof",
    Callback = function(value) s.Misc.DeviceSpoofer = value SpoofDevice() end
})
miscSec:Dropdown({
    Name = "Device",
    Items = { "PC", "Mobile", "Gamepad" },
    Default = s.Misc.Device,
    Flag = "misc_device",
    Callback = function(value) s.Misc.Device = value SpoofDevice() end
})

local miscTab = window:Tab({ Name = "Misc", Icon = "settings" })
local Config = miscTab:SubTab({ Name = "Config", Icon = "save" })
Config:ThemeConfig({})

task.delay(5, function()
    if Icon then Icon:Destroy() end
    CursorIcon2.Visible = false
end)

zolar:Notification({
    Name = "Aural Hub loaded",
    Description = "Press G to toggle the menu.",
    Icon = "check",
    Duration = 3
})
print("Aural loaded thanks for purchasing")
print("royce1 bypass is active dont dw abt this")
